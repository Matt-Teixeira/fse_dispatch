const { map_fse_fields, normalizeFieldsForSharePoint } = require("./map-fse-fileds");
const send_dispatch_channel = require("./send-to-channel");

module.exports = { map_fse_fields, normalizeFieldsForSharePoint, send_dispatch_channel };
